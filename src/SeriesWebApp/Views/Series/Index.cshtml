@using TddDemo
@model IEnumerable<Serie>
<h2>Series</h2>
<table class="table">
    <tr><th>Title</th></tr>
    @foreach (var item in Model)
    {
        <tr><td>@Html.ActionLink(item.Title, "Details", new { item.Id })</td></tr>
    }
</table>